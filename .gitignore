# Python app (server-side, not part of addon)
/*.py
/__pycache__/
/*.pyc
/*.pyo
/requirements.txt
/templates/

# Data / secrets
data.db
.nexus_token_cache
.env

# IDE
.idea/
.vscode/

# Claude
.claude/
CLAUDE.md

# Signing artifacts (signed copies are attached to GitHub releases)
web-ext-artifacts/

# AMO submission bookkeeping + signed copies (AMO distributes those)
.amo-upload-uuid
nexus_legacy_survey_tracker-*.xpi

# Chrome package (built alongside the xpi; distributed via the Web Store)
nexus-accounting-*.zip

Stats.txt

# Working notes (not part of the addon)
report_dead_code.md
todolist.md
